{-| The Array type.

# Type
@docs Array<a>, Create

# Equality
@docs eq, ne

# Comparison
@docs lt, le, gt, ge

-}

type Array

builtin type Array<a>

buitin fn Create<a>(size: int, default: a) : Array<a>

fn Equal(x: Array<a>, y: Array<a>) : bool

fn Count<a>(x: Array<a>) : int

fn IsEmpty(x: Array<a>) : bool
    Count(x) == 0

fn Append<a>(x: Array<a>, y: Array<a>) : Array<a>
